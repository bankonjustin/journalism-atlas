
# Dolt database files (added by bd init)
.dolt/
*.db

# Environment / secrets
.env
.env.*

# Atlas Wire working files (local only, never deploy)
wire_queue_raw_*.json
wire_queue_scored_*.json
wire_approved_*.json
